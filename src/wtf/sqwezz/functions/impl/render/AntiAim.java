package wtf.sqwezz.functions.impl.render;

import com.google.common.eventbus.Subscribe;
import net.minecraft.client.Minecraft;
import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.util.math.MathHelper;



            // Увеличиваем угол поворота головы
     //       headYaw += ROTATION_SPEED * Minecraft.getInstance().getFrameTime();
//
            // Ограничиваем угол поворота
      //      headYaw = MathHelper.wrapDegrees(headYaw);

            // Применяем поворот к модели игрока
    //        event.getMatrixStack().multiply(event.getMatrixStack().last().pose());
      //      event.getMatrixStack().translate(0, 0, 0.1f);
         //   event.getMatrixStack().rotate(event.getMatrixStack().last().pose(), 0, MathHelper.deg2Rad(headYaw), 0);
            //event.getMatrixStack().translate(0, 0, -0.1f);
    //    }
  //  }
//}